<Window x:Class="MainProject.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MainProject"
        mc:Ignorable="d"
                Title="Menu"
        Width="400" Height="400"
        Background="White"
        Name="Menu">
    <Grid>

        <TextBlock Text="Play Uno"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   FontSize="48"
                   FontWeight="Bold"
                   Foreground="#6A4E23"
                   Margin="0,50,0,40"/>

        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,150,0,0">

            <Button Content="Lancer une Partie"
                    Width="300"
                    Height="50"
                    Margin="10"
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="White"
                    Background="#C2B280"
                    BorderBrush="#8C6E4B"
                    BorderThickness="1"
                    Command="{Binding StartGameCommand}"/>

            <Button Content="Voir les Statistiques"
                    Width="300"
                    Height="50"
                    Margin="10"
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="White"
                    Background="#C2B280"
                    BorderBrush="#8C6E4B"
                    BorderThickness="1"
                    Command="{Binding ViewStatsCommand}"/>

            <Button Content="Quitter"
                    Width="300"
                    Height="50"
                    Margin="10"
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="White"
                    Background="#D76C4E"
                    BorderBrush="#B14F39"
                    BorderThickness="1"
                    Command="{Binding QuitCommand}"/>
        </StackPanel>
    </Grid>
</Window>